class Compute {
    
    public int largest(int arr[], int n)
    {
        int max=arr[0];
       //loop to traverse an array
       for(int i=1;i<n;i++)
       {
           if(max<arr[i])
           max=arr[i];
       }
       return max;
    }
}
